마크다운 작성 시 실무와 GitHub README 작성에서 **가장 자주 쓰이고 핵심이 되는 문법** 정리입니다.

---

### 1. 제목 (Headers)

줄 앞에 `#`을 붙여 1단계부터 6단계까지의 제목을 만듭니다. `#` 뒤에는 반드시 한 칸을 띄워야 합니다.

```markdown
# 1단계 제목 (가장 큼)
## 2단계 제목
### 3단계 제목

```

---

### 2. 텍스트 서식 (Text Formatting)

기본적인 텍스트 강조 서식입니다.

```markdown
**굵은 글씨**
*기울임꼴*
~~취소선~~

```

---

### 3. 목록 (Lists)

순서가 있는 목록과 순서가 없는 목록을 나타냅니다. 하위 항목을 만들 때는 **Tab** 또는 **스페이스바 2~4칸**을 들여씁니다.

```markdown
# 순서 없는 목록
- 첫 번째 항목
  - 하위 항목
* 별표(*)로도 작성 가능합니다

# 순서 있는 목록
1. 첫 번째
2. 두 번째

```

---

### 4. 링크 및 이미지 (Links & Images)

링크와 이미지 표기는 느낌표(`!`)의 유무로 구분됩니다.

```markdown
# 링크
[표시할 텍스트](https://example.com)

# 이미지 (!를 붙입니다)
![대체 텍스트](https://example.com/image.png)

```

---

### 5. 코드 (Code & Code Block)

단어 강조에는 백틱(```) 1개, 여러 줄 코드에는 백틱 3개(`````)를 사용합니다. 백틱 3개 옆에 언어명을 적으면 문법 하이라이팅이 적용됩니다.

```markdown
# 인라인 코드
변수명은 `count`로 지정합니다.
```
# 코드 블록
```cpp
#include <iostream>

int main() {
    std::cout << "Hello World";
    return 0;
}

```

---

### 6. 인용구 (Blockquotes)
문장 앞에 `>`를 사용합니다.

```markdown
> 인용할 내용이나 주의할 점을 적을 때 사용합니다.

```

---

### 7. 표 (Tables)

파이프(`|`) 기호로 열을 구분하고, 두 번째 줄에 하이픈(`-`)을 사용하여 생성합니다.

```markdown
| 항목 | 설명 | 비고 |
| --- | --- | --- |
| C++ | 주요 언어 | 실행 속도 빠름 |
| README | 마크다운 | 깃허브 메인 문서 |

```

---

### 8. 체크리스트 (Task Lists)

GitHub에서 대시(`-`)와 대괄호(`[ ]`, `[x]`)를 조합해 체크박스를 만듭니다.

```markdown
- [x] 완료된 작업
- [ ] 진행 예정 작업

```
